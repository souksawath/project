rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write for authenticated users or public app access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
